# Arbor

Arbor is a cross-platform tree command that modernizes the Windows `tree` utility.

## Install

```bash
dotnet tool install -g Arbor
```

## Update

```bash
dotnet tool update -g Arbor
```

## Usage

```bash
arbor tree . --files
```

Use `arbor --help` for more options.
